<div>
  <div>
    <h1>
      List of users
    </h1>
  </div>
</div>

<div>
  <div>
    <table border="1">
      <tr>
        <th>
          Username
        </th>

        <th>
          Privare?
        </th>

        <th></th>

    <% if @current_user != nil %>
        <th></th>
    <% end %>

      </tr>

      <tr>
    <% @list_of_users.each do |a_user| %>
        <td>
          <%= a_user.username %>
        </td>

        <td>
          <!--<%= a_user.private %>-->
          <%= humanize_boolean(a_user.private) %>
        </td>

    <% if @current_user != nil %>
        <td>
        <form action="/insert_follow_request" method="post">
          <!--<label for="recipient_id_box">
            Recipient
          </label>-->

        <%# follow = FollowRequest.where({ :id => a_user.recipient_id }).at(0) %>
        <%#= follow.status %>

          <input type="hidden" id="recipient_id_box" name="query_recipient_id" value="<%# follow_requests_sent.recipient_id %>">

        <!--<label for="sender_id_box">
          Sender
        </label>-->

          <input type="hidden" id="sender_id_box" name="query_sender_id" value="<%# @the_follow_request.sender_id %>">

      <!--<div>
        <label for="status_box">
          Status
        </label>-->
  
          <input type="hidden" id="status_box" name="query_status" value="<%# @the_follow_request.status %>">

        <button>
          Follow
        </button>

      <!--<button>
        Create follow request
      </button>-->
      </form>
      </td>

          <!--<form action="/insert_follow_request/<% a_user.id %>" method="post">
              <input type="hidden" name="query_recipient_id" value="<% a_user.id %>">

              <button>
                Follow
              </button>
            </form>-->
        <% end %>    
        <!-- make this status and follow button show conditionally -->
        <!--<td>
            Request sent.
        <a href="/delete_follow_request/<% a_user.id %>">Cancel</a>
        </td>-->

        <td>
          <a href="/users/<%= a_user.id %>">Show details</a>
        </td>
      </tr>
    </table>
  </div>
</div>
<% end %>

<hr>

